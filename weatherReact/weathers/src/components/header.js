import React from 'react';

const Header = () => {
    return(
        <nav className="navbar navbar-dark bg-dark">
           <span className="navbar-brand h1">Weather App</span>
        </nav>
    );
}

export default Header;