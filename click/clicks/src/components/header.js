import React from 'react';

const Header = () => {
    return(
        <nav className="navbar navbar-dark bg-dark">
            <h1 className="navbar-brand" style={{fontSize: 40}}>Clicks</h1>
        </nav>
    );
}

export default Header;