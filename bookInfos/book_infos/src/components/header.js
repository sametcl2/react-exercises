import React from "react"

const Header = () => {
    return(
        <header className="navbar navbar-dark bg-dark">
            <h1 className="navbar-brand">Book Infos</h1>
        </header>
    );
}

export default Header;